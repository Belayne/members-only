const LocalStrategy = require("passport-local").Strategy;


const verifyFunction = async (username, password, done) => {
    
}

module.exports.Strategy = new LocalStrategy(async (username, password, done) => {

})